%h2 The Muse Project
%p Post images that inspire you.

- @post.each do |post|
  = link_to (image_tag post.image.url(:small)), post
  %h2= link_to post.title, post
  %p= pluralize(post.comments.count, "Comment")
  %br


= link_to "Add new post", new_post_path
